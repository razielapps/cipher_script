# the third version of the cipher script

#TODO:
     #TODO 1:
        #* allow a user to enter a message
        #* encrypt the message into a cipher text
        #* (v3) now re encypt the cipher text into bits (numbers)
        #* allow a user to enter number
        #* decrypt to -> cipher -> plain message